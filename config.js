// Session Zero — runtime config.
// Paste the public URL of your Railway rooms API here (no trailing slash) to
// enable shareable lists + async voting. Leave empty and the site works
// exactly as before (local in-room voting only).
window.SESSIONZERO_API = 'https://sessionzero-rooms-production.up.railway.app';
